{% layout "layout.liquid"%} {% block content %}
<div><a href="/fruits/new"><button>creat new fruit</button></a>
  {% for fruit in fruits %}
  <article>
     <a href="/fruits/{{ fruit._id }}"/>
    <h2>
      {{fruit.name}} - 
         {% if fruit.readyToEat == true %}
        Ripe
         {% else %}
       Not Ripe
        {% endif %}
    </h2>
  
  </article>
  {% endfor %}
</div>

{% endblock %}
